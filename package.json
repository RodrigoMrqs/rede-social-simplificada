{
  "name": "rede-social-simplificada",
  "private": true,
  "workspaces": ["web", "server"],
  "scripts": {
    "web": "npm run dev --workspace=web",
    "server": "npm run dev --workspace=server"
  }
}
