// TODO: Implement AdminModerationPage
// UC-20 — moderar post: ocultar (hidden_at) ou deletar (deleted_at) + registrar em moderation_logs
// UC-21 — suspender (expires_at preenchido) ou banir (expires_at NULL) usuário via user_sanctions
//          revogar todas as sessões ativas do usuário sancionado
// Redirecionar para /feed se usuário não for admin

export default function AdminModerationPage() {
  return <div>Moderação — placeholder</div>;
}
