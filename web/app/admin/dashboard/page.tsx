// TODO: Implement AdminDashboardPage
// UC-19 — rota protegida: verificar se usuário está na tabela `admins` (não por campo role)
// UC-22 — métricas via queries de agregação: total de usuários, posts, suspensões ativas, etc.
// Redirecionar para /feed se usuário não for admin

export default function AdminDashboardPage() {
  return <div>Painel Admin — placeholder</div>;
}
