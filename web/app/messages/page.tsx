// TODO: Implement MessagesPage
// UC-26 — lista todas as conversas do usuário autenticado, ordenadas pela mensagem mais recente
// Exibir: avatar + nome do outro participante, prévia da última mensagem, badge de não lidas
// Botão para iniciar nova conversa (buscar usuário → POST /messages)

export default function MessagesPage() {
  return <div>Mensagens — placeholder</div>;
}
