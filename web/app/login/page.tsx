// TODO: Implement LoginPage
// UC-02 — valida credenciais, grava sessão em `sessions`, redireciona para /feed
// Campos: username, password
// Em sucesso: salvar token em AuthContext e redirecionar para /feed
// Em erro: exibir mensagem de erro inline

export default function LoginPage() {
  return <div>Login — placeholder</div>;
}
