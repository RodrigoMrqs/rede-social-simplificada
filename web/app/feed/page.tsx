// TODO: Implement FeedPage
// UC-16 — lista posts + reposts dos seguidos, ordenados por created_at DESC, paginação por cursor
// UC-13 — curtir/descurtir post (botão inline em cada PostCard)
// UC-14 — link para /post/[id] para comentar
// UC-15 — repostar (modal ou inline)

export default function FeedPage() {
  return <div>Feed — placeholder</div>;
}
