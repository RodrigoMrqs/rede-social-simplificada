// TODO: Implement RegisterPage
// UC-01 — cria conta + notification_preferences, redireciona para /feed
// Campos: username, displayName, password, confirmPassword
// Em sucesso: salvar token em AuthContext e redirecionar para /feed
// Em erro: exibir mensagem de erro inline

export default function RegisterPage() {
  return <div>Cadastro — placeholder</div>;
}
