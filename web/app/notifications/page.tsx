// TODO: Implement NotificationsPage
// UC-17 — lista notificações do usuário autenticado por recipient_id, desc
// Badge no link de navegação mostra contagem de não lidas (read_at IS NULL)
// Botão "Marcar todas como lidas" → notificationService.markAllRead()
// Tipos de notificação: follow, like, comment, repost, mention

export default function NotificationsPage() {
  return <div>Notificações — placeholder</div>;
}
